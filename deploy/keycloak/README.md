# Keycloak Configuration

Place your realm configuration file (e.g., `realm-export.json`) in this directory.

When using the `docker-compose.yml` in the parent directory, this file will be imported on startup.
